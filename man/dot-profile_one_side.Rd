% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile_helpers.R
\name{.profile_one_side}
\alias{.profile_one_side}
\title{Profile one side of a likelihood profile (internal)}
\usage{
.profile_one_side(
  direction,
  increment,
  max_steps,
  profile_parameter,
  optim_param_vector,
  env_dat,
  occ,
  mask,
  num_threads,
  optim_ll,
  thresh,
  base_control,
  start_full = optim_param_vector,
  invh_lt = NULL,
  verbose = FALSE
)
}
\arguments{
\item{direction}{Integer. -1 (left) or +1 (right).}

\item{increment}{Numeric. Step size on math scale for this side.}

\item{max_steps}{Integer. Maximum iterations for this side.}

\item{profile_parameter}{Character. Name of the parameter to profile. Profiles
are done on the math scale.}

\item{optim_param_vector}{Named numeric. MLE parameters on math scale.}

\item{env_dat}{3D array (locations x time x variables).}

\item{occ}{Logical, either 0 or 1, vector (length = number of locations).}

\item{mask}{Named numeric or NULL. Parameters kept fixed (math scale).}

\item{num_threads}{Integer. Threads used internally by log-likelihood.}

\item{base_control}{Named list. Control passed to \code{ucminf::ucminf(control = ...)}.
User-specified entries should be merged in the caller (see \code{profile_likelihood}).}

\item{start_full}{Named numeric. Full warm-start parameter vector.
Defaults to \code{optim_param_vector}.}

\item{invh_lt}{Optional numeric. Lower triangle of the inverse Hessian for warm-start.}

\item{verbose}{Logical. If \code{TRUE}, prints compact progress messages; otherwise silent.}
}
\value{
A list with elements \code{ll}, \code{vals}, \code{fulls}, \code{conv},
  \code{last_full}, \code{last_invh}, \code{steps}, and \code{crossed}.
}
\description{
Fixes one parameter on the math scale and re-optimizes the remaining
parameters along a single direction (left/right) until the LR threshold
is reached or a step cap is hit.
}
\seealso{
\code{\link{profile_likelihood}}
}
\keyword{internal}
