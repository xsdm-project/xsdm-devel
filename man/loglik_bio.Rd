% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loglik_bio.R
\name{loglik_bio}
\alias{loglik_bio}
\title{Log-likelihood function for the xsdm model, parameters on the biological
scale.}
\usage{
loglik_bio(
  env_dat,
  occ,
  mu,
  sigltil,
  sigrtil,
  o_mat,
  ctil,
  pd,
  return_prob = FALSE,
  sum_log_p = TRUE,
  num_threads = RcppParallel::defaultNumThreads()
)
}
\arguments{
\item{env_dat}{The environmental data array, dimensions
\code{n_loc x n_time x p} (number of locations x time-series length x number
of environmental variables). Must be a 3-dimensional array with no missing
values.}

\item{occ}{Presence/pseudo-absence binary vector. Same length as dimension 1 of
\code{env_dat}.}

\item{mu}{Vector of optimal environmental values. Length \code{p=dim(env_dat)[3]}.
Unconstrained real numbers.}

\item{sigltil}{Vector specifying width of the growth-environment function.
Length \code{p=dim(env_dat)[3]}. Positive real numbers, \code{Inf} entries also allowed.}

\item{sigrtil}{Vector specifying width of the growth-environment function.
Length \code{p=dim(env_dat)[3]}. Positive real numbers, \code{Inf} entries also allowed.}

\item{o_mat}{An orthogonal matrix, dimensions \code{p} by \code{p}.}

\item{ctil}{Scalar. Relates to the center of the detection-link function.}

\item{pd}{Maximum probability of detection of the species. Parameter between
0 and 1.}

\item{return_prob}{Logical (default FALSE). Flag to return likelihood instead
of log-likelihood.}

\item{sum_log_p}{Logical (default TRUE). If FALSE, returns the individual
log-likelihoods (or likelihoods, if \code{return_prob} is TRUE) associated with the
individual locations, instead of their sum (product, if \code{return_prob} is TRUE).}

\item{num_threads}{Number of threads for parallel computation. Defaults to
\code{RcppParallel::defaultNumThreads()}.}
}
\value{
A single value, the log-likelihood (or the likelihood, if \code{return_prob}
is TRUE); or a vector of location specific values of \code{sum_log_p} is FALSE.
}
\description{
Computes the log-likelihood for the xsdm model given environmental data, a
vector of occurrences and pseudo-absences, and model parameters on the
biological scale.
}
\details{
This is a thin R wrapper around the C++ implementation \code{loglik_bio_cpp};
the optimizer hot path (\code{sum_log_p = TRUE}, \code{return_prob = FALSE})
is pure C++. The non-default flag combinations
(\code{sum_log_p = FALSE} or \code{return_prob = TRUE}) are computed by
delegating to the C++-backed \code{log_prob_detect} and reducing in R.
A pure-R reference implementation, \code{loglik_bio_r}, is kept internal
to the package and is used only by the parity tests in
\code{tests/testthat/test-loglik_bio_r_vs_cpp.R}.
}
\examples{
ll <- loglik_bio(
  env_dat = example_1$env_array,
  occ = example_1$occ_df$presence,
  mu = example_1$par_list$mu,
  sigltil = example_1$par_list$sigltil,
  sigrtil = example_1$par_list$sigrtil,
  o_mat = example_1$par_list$o_mat,
  ctil = example_1$par_list$ctil,
  pd = example_1$par_list$pd
)
ll
}
