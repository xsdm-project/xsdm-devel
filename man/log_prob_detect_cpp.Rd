% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{log_prob_detect_cpp}
\alias{log_prob_detect_cpp}
\title{Compute log detection probabilities from a flat environmental data vector}
\usage{
log_prob_detect_cpp(
  env_dat_vec,
  env_dat_dims,
  mu,
  sigltil,
  sigrtil,
  o_mat,
  ctil,
  pd,
  return_prob = FALSE,
  num_threads = 0L
)
}
\arguments{
\item{env_dat_vec}{Numeric vector.  Column-major flat representation of a
3-D array with logical dimensions \code{c(n_loc, ts_length, p)}: variable
\code{k} (1-indexed) occupies positions
\code{(k-1)*n_loc*ts_length + 1} to \code{k*n_loc*ts_length},
and within that block pixels (locations) vary fastest.}

\item{env_dat_dims}{Integer vector of length 3: \code{c(n_loc, ts_length, p)}.}

\item{mu}{Numeric vector of length \code{p}.  Optimal environmental values.}

\item{sigltil}{Numeric vector of length \code{p}.  Positive; \code{Inf}
entries are allowed (treated as zero inverse-scale).}

\item{sigrtil}{Numeric vector of length \code{p}.  Positive; \code{Inf}
entries are allowed.}

\item{o_mat}{Numeric matrix, \code{p x p} orthogonal.}

\item{ctil}{Scalar.  Center of the detection-link function.}

\item{pd}{Scalar in \code{(0, 1]}.  Maximum probability of detection.}

\item{return_prob}{Logical.  If \code{TRUE}, return probabilities; if
\code{FALSE} (default) return log-probabilities.}

\item{num_threads}{Integer.  Number of parallel threads.  \code{0}
(default) uses \code{RcppParallel::defaultNumThreads()}.}
}
\value{
Numeric vector of length \code{n_loc}.
}
\description{
C++ implementation of \code{log_prob_detect()} that accepts environmental
data as a flat numeric vector with explicit dimension metadata.  This
signature is designed for block-by-block raster evaluation where each
block is passed as a contiguous vector rather than a 3-D R array.
}
\details{
Collapses the R call chain
\code{like_neg_ltsgr_cpp() -> like_ltsg()} into a single xtensor-accelerated
C++ function.
}
