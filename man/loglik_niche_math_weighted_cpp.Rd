% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loglik_niche_math_weighted_cpp.R
\name{loglik_niche_math_weighted_cpp}
\alias{loglik_niche_math_weighted_cpp}
\title{Negative log-likelihood (weighted-normal, math scale, Cholesky version)}
\usage{
loglik_niche_math_weighted_cpp(
  theta,
  env_occ,
  env_m,
  eta = 1,
  neg = TRUE,
  m_subsample = NULL,
  m_kde_subsample = NULL,
  seed = NULL
)
}
\arguments{
\item{theta}{Numeric vector of unconstrained parameters:
\itemize{
  \item First `p` elements: `mu` (centroid)
  \item Next `p` elements: `log_sigma` (log of standard deviations)
  \item Last `p*(p-1)/2` elements: parameters for the correlation matrix
        (C‑vine partial correlations, passed to `cvine_cholesky`).
}}

\item{env_occ}{Data frame with environmental values at presence points (size `n_occ x p`).}

\item{env_m}{Data frame with environmental values from the accessibility area M
(size `n_m x p`); used as the background sample.}

\item{eta}{Numeric, shape parameter for the LKJ‑C‑vine prior (default 1).}

\item{neg}{Logical. If `TRUE` (default) returns the negative log‑likelihood
(suitable for minimization); if `FALSE` returns the positive log‑likelihood.}

\item{m_subsample}{Optional integer or fraction for subsampling the denominator
from `env_m`. If `NULL` (default), uses all rows. If `0 < value < 1`, uses
`floor(value * nrow(env_m))` rows; if `>= 1`, uses `min(value, nrow(env_m))` rows.}

\item{m_kde_subsample}{Optional integer or fraction for subsampling the KDE reference
set from `env_m`. Same rules as `m_subsample`.}

\item{seed}{Optional integer seed for reproducible subsampling.}
}
\value{
Scalar numeric value: the (negative) log-likelihood.
}
\description{
Computes the weighted-normal log-likelihood (Jiménez & Soberón 2022) using
the unconstrained ("math") parameterization and passing the Cholesky factor
directly to C++ for efficient calculation. Handles optional subsampling of
the M background for the denominator and for KDE estimation.
}
\examples{
\dontrun{
theta <- start_theta(example_env_occ_2d)
# Weighted log-likelihood (math scale) using Cholesky version
ll <- loglik_niche_math_weighted_cpp(theta,
                                     env_occ = example_env_occ_2d,
                                     env_m   = example_env_m_2d,
                                     m_subsample = 2000,
                                     m_kde_subsample = 5000,
                                     seed = 123)
print(ll)
}
}
