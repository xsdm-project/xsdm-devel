% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_likelihood.R
\name{optimize_likelihood}
\alias{optimize_likelihood}
\title{Optimize the xsdm log-likelihood from multiple starts (ucminfcpp)}
\usage{
optimize_likelihood(
  env_dat,
  occ,
  mask = NULL,
  num_starts = 100L,
  breadth = 1,
  parallel = FALSE,
  num_threads = RcppParallel::defaultNumThreads(),
  control = list(),
  verbose = FALSE
)
}
\arguments{
\item{env_dat}{3D array: (locations x time x variables), no NAs.}

\item{occ}{Logical or integerish 0/1 vector, length = nrow(env_dat).}

\item{mask}{NULL or named numeric math-scale values to fix.}

\item{num_starts}{Integer. Number of starting points. Default 100.}

\item{breadth}{Scalar in \code{[0, 1]} controlling how wide the search
ranges are around their (fixed, data-driven) center. \code{breadth = 1}
(the default) reproduces the pre-v0.3 behaviour that corresponded to
\code{quant_vec = c(0.1, 0.5, 0.9)}; \code{breadth = 0} collapses
every range to essentially a single point, equivalent to
\code{quant_vec = c(0.5 - 1e-6, 0.5, 0.5 + 1e-6)}. Values in between
interpolate linearly.}

\item{parallel}{Logical. If TRUE, distribute starts via \pkg{future}/\pkg{furrr}.}

\item{num_threads}{Integer >=1. Threads used inside \code{loglik_math}.
If \code{parallel=TRUE}, consider \code{num_threads=1}.}

\item{control}{Named list merged into ucminfcpp control. User wins over defaults.}

\item{verbose}{Logical. If TRUE, prints compact progress messages.}
}
\value{
A list with:
\itemize{
  \item \code{solutions}: data.frame sorted by decreasing \code{loglik},
        columns: \code{start_id}, \code{loglik}, \code{convergence},
        and list-column \code{full_par} (complete math-scale vector).
  \item \code{best}: list with \code{par} (full math-scale vector),
        \code{loglik}, and \code{convergence}.
}
}
\description{
Runs multiple ucminfcpp optimizations from starting values generated by
\code{start_parms()}, optionally in parallel. Returns one row per start with
the achieved log-likelihood, convergence code, and a full math-scale parameter
vector (list-column) reconstructed with \code{mask}.
}
\examples{
optimize_likelihood(
  env_dat = examples$env_array[1:4, , ],
  occ = examples$occ_vec[1:4],
  num_starts = 4L
)
}
