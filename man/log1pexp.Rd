% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log1pexp.R
\name{log1pexp}
\alias{log1pexp}
\title{Numerically stable `log(1 + exp(x))`}
\usage{
log1pexp(x, c0 = -37, c1 = 18, c2 = 33.3)
}
\arguments{
\item{x}{Numeric vector. `NA` values are preserved.}

\item{c0, c1, c2}{Numeric scalars defining the switch points between
four asymptotically optimal formulas. Defaults (-37, 18, 33.3)
are from Mächler (2012) and should not normally be changed.}
}
\value{
A numeric vector the same length as `x` with
  \eqn{\log(1 + \exp(x))}.
}
\description{
Computes \eqn{\log(1 + \exp(x))} accurately for any real `x`,
avoiding overflow as `x -> +Inf` and catastrophic cancellation as
`x -> -Inf`.
}
\examples{
x <- seq(-40, 40, by = 10)
cbind(x, log1p(exp(x)), log1pexp(x))
}
\references{
Mächler, M. (2012). *Accurately Computing
  log(1 − exp(− |a|)).* CRAN package `copula` vignette.
}
\seealso{
\code{\link{log1mexp}}, \code{\link{log1p}},  \code{\link{expm1}}
}
